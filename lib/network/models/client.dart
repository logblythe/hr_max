class HrClient {
  String name;
  String status;
  String baseUrl;

  HrClient({String name, String status, String baseUrl})
      : this.name = name,
        this.status = status,
        this.baseUrl = baseUrl;
}
